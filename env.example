# Database connection string for Aiven PostgreSQL
# Replace <USER>, <PASSWORD>, <HOST>, <PORT>, and <DBNAME> with your own values
DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DBNAME>?sslmode=require